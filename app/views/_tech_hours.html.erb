<div class="jumbotron" id="tech_hours">
  <div class="section_header">
    <h2 class="section_title">Tech Hours</h2>
    <p class="white_text"><strike>I'm also tracking my software development
      hours.</strike></p>
    <p class="white_text">I tracked my software development hours for a while,
      but it made my work feel more like a chore and less like fun. Live and
      learn : )</p>
  </div>

  <div class="container">
    <span class="text-nowrap">
      <table class="table" style="color: #fff;">
        <% @weeks = Week.all %>
        <thead>
          <tr>
            <th>Week</th>
            <th>Tech Hours</th>
          </tr>
        </thead>
        <tbody>
          <% @weeks.each do |week| %>
            <tr>
              <td>
                <%= week.name %>
              </td>
              <td>
                <% total_hours = week.work_hours + week.leisure_tech_hours %>
                <div class="hours_bar" style="width:<%= total_hours.round * 10 %>px;">
                  <%= total_hours %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </span>
  </div>
</div>
